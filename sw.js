/* Service worker порталу НавігаторПМГ26.
   Стратегія: HTML — network-first (щоб оновлення доїжджали одразу),
   статика — cache-first із фоновим оновленням (версії ?v= у HTML).
   Запити до Supabase та інших доменів не перехоплюються. */
// v7 (29.07.2026): підняли версію, щоб activate вимів старі записи —
// інакше оновлені файли зі зміненим ?v= доїжджають, а сусідні лишаються старими
// v8 (30.07.2026): фірмовий фон НСЗУ — brand.css, кропи фону, круглі іконки
// v10 (03.08.2026): новий розділ «Посади» (ДКХП-78 + коди НСЗУ + кадрові
//   вимоги пакетів) і новий пункт у вкладці «Коди» — піднято, щоб activate
//   вимів старі записи auth-v2.js і map.js
// v11 (04.08.2026): назви у вимогах пакета стали посиланнями (spec-links.js)
// v12 (04.08.2026): розділ «Обладнання». Тут же нагадування на майбутнє:
//   дані розділів (/classifiers/data/…) ходять БЕЗ ?v=, а стратегія нижче —
//   cache-first, тож перезібраний JSON доїжджає лише з другого заходу. Змінив
//   дані розділу — піднімай CACHE, інакше сторінка малює вчорашній стан
//   свіжим кодом (спіймано на equipment_cards.json).
// v9 (30.07.2026): чотири виправлення «сторінка відкрилась з другого разу» —
//   1) прибрано skipWaiting()/clients.claim() — ГОЛОВНА причина, див. нижче;
//   2) невдала навігація більше НЕ підміняє сторінку головною;
//   3) мережа для навігації обмежена таймаутом, далі йде кеш цієї ж сторінки;
//   4) URL із cache-buster (?t=1785442158836) не осідають у Cache Storage.
// v15 (05.08.2026): вкладка «Коди» стала «Довідники» з підменю (Коди ·
//   Обладнання · Посади) — піднято, щоб activate вимів старі auth-v2.js/css.
//   Там же карта порталу: довідники винесено в окремий кластер із гніздами
// v16 (05.08.2026): перезібрано дані двох розділів — mapping/data (коефіцієнти
//   ДСГ тепер прив'язані до колонок додатків) і postanova/data/resolution_1808
//   (додано таблиці додатків 1–2 з розміткою колонок). Обидві теки ходять без
//   ?v=, тож без підняття CACHE сторінки малювали б учорашні числа.
//   Там же полагоджено фонове оновлення кешу (waitUntil у fetch, див. нижче):
//   без нього дані, віддані з кешу, не оновлювалися ВЗАГАЛІ — перевірено на
//   services.json, у Cache Storage лежала копія, старша за файл на сервері.
// v17 (05.08.2026): свіжа вивантажка договорів ЗОЗ (data/contracts*.json —
//   теж без ?v=). У новій вивантажці прийшли ПІБ і посади керівників — у JSON
//   їх не беремо: репозиторій публічний, історію git не відмотати.
// v18 (05.08.2026): у contracts*.json з'явилися source_date/built_at, а на
//   сторінці договорів і в Паспорті — бейдж «База станом на …».
// v19 (05.08.2026): новий розділ «Хто це лікує» (zoz-poshuk) — піднято, щоб
//   activate вимів старий auth-v2.js із меню без цього пункту.
// v20 (05.08.2026): з providers.json прибрано пошту надавачів. Бамп тут
//   ОБОВ'ЯЗКОВИЙ: zoz-poshuk/data ходить без ?v= і лежить у кеші cache-first,
//   тож без нового CACHE у людини, яка вже відкривала розділ, залишилася б
//   стара копія файлу з поштами.
// v21 (05.08.2026): у пошук закладів додано коди медичних послуг ЕСОЗ, у
//   таблиці співставлення ці коди стали посиланнями на пошук.
// v22 (05.08.2026): полагоджено НК 026 — у 90904-00 більше не злипається
//   додатковий перелік послуг ЕСОЗ; звідти ж узято 502 назви цих послуг.
//   classifiers/data і zoz-poshuk/data ходять без ?v=, тож бамп обов'язковий.
// v23 (05.08.2026): два нові роз'яснення (лист 18388/8-15-26 і його додаток),
//   у реєстрі роз'яснень запрацював пошук за кодом, у codes_tables вперше
//   з'явився LOINC. Дані розділу ходять без ?v=.
// v24 (05.08.2026): вкладка «Пакети» стала дропдауном (ПМГ + пілоти), на
//   пілотах прибрано смугу доручень, паспорт більше не підміняє пілотний
//   номер пакетом 1.
// v25 (05.08.2026): злито дві системи кольорових токенів — значення ролей
//   тепер в одному місці, --p-* лишилися аліасами базових.
// v26 (05.08.2026): світлі плями в темній темі — бейдж ролі більше не
//   малюється інлайновими стилями з auth-v2.js (інлайн бив тему), фільтри
//   у структурі департаменту перестали бути білими, кнопка «Увійти» й
//   «Очистити» в роз'ясненнях пішли на ролі. Бамп потрібен, бо стара
//   auth-v2.js із кешу знову зафарбувала б бейдж поверх нового CSS.
// v27 (05.08.2026): обхід усіх 44 сторінок у темній темі — плашки категорій
//   у новинах, пігулки в нормативці, позначки формату в збірнику, швидкі
//   фільтри Інфоцентру, статус «не відмітився» у структурі, кнопки
//   «Очистити» в розділах і колір стежки навігації.
// v28 (05.08.2026): акцент розділено на дві ролі — --accent для тексту й
//   обведень, --accent-fill для заливки під білим текстом (у темній темі
//   глибший #0369a1). Первинні кнопки й активні чіпи стали читаними.
// v29 (06.08.2026): нова сторінка algorithms/rehab.html (наказ НСЗУ № 182) з
//   даними rehab_182.json, нове гніздо «Накази НСЗУ» в навігації та мітки ?v=
//   спільних файлів на всіх сторінках.
// v30 (06.08.2026): посади — вимоги-переліки «та/або» більше не губляться
//   (перезібрано дані posady, у картках позначка «одна з N посад переліку»).
// v31 (07.08.2026): новий розділ «Рентген і ДІВ» (rentgen) — 22 акти дозвільної
//   системи з деревом структури та зв'язкою «пакет → ДІВ → дозвіл». Бамп
//   обов'язковий із двох причин: activate має вимести старий auth-v2.js із меню
//   без цього пункту (як у v19 для zoz-poshuk), і rentgen/data ходить без ?v=
//   під cache-first.
// v32 (07.08.2026): у розділі «Рентген» перезібрано дані (коди ЕСОЗ за наказом
//   № 697 у вимогах пакета, позначення пунктів, дотичних до ПМГ) і полагоджено
//   макет — портальний .layout є двоколонковим grid, тож розділ мусить скинути
//   display, інакше півекрана порожнє. Дані rentgen/data ходять без ?v=.
// v33 (07.08.2026): доїхала зміна № 948 до постанови 1808 — банер актуальної
//   редакції на сторінці постанови показував 13.06.2026, хоча чинна редакція
//   від 16.07.2026. Файл postanova/data/document_status.json просто не був
//   задеплоєний; postanova/data ходить без ?v= під cache-first, тож без бампу
//   у того, хто вже відкривав сторінку, лишився б старий банер.
// v34 (08.08.2026): перебудоване верхнє меню — «Роз'яснення», «Паспорт пакета»
//   й «Архів пакетів» пішли у вкладку «Пакети», «Карта» — у «Сервіси», де
//   з'явилося друге коліно (СКО-Д · Взаємодія · Інструменти), а «Структура»
//   стала «Структурою Департаменту». Бамп обов'язковий: без нього activate не
//   вимете стару auth-v2.js, і в того, хто вже заходив, лишиться старе меню.
// v35 (09.08.2026): перезібрано дані двох розділів — mapping/data і drg/data.
//   Колонка «Медична послуга» пакета 19 містить діапазони МКХ-10, а не ДСГ
//   («F00 - F09. Органічні… психічні розлади»), і збірка мапінгу читала їх
//   шаблоном коду ДСГ. Через це 17 психіатричних послуг лишалися взагалі без
//   назви, а псевдокод F09 збігався з ДСГ F09 додатка 1 («Інші кардіоторакальні
//   операції…») і чіпляв її вагу 3,296 — тег коефіцієнта було видно в картці
//   послуги та в паспортах НК 025 і НК 026. Бамп обов'язковий: mapping/data і
//   drg/data ходять без ?v= під cache-first, тож без нового CACHE у того, хто
//   вже відкривав розділи, лишилися б старі числа.
// v36 (10.08.2026): перезібрано корпус розділу «Рентген» — 22 акти стали 38
//   (доданий клінічний контур мамографії: скринінг РМЗ, стандарти МД,
//   клінічні настанови), 2 801 вузол став 4 181. Бамп обов'язковий з тієї ж
//   причини: rentgen/data ходить без ?v= під cache-first.
// v37 (10.08.2026): новий розділ «Спеціальності та посади» (Додаток 7 до
//   Ліцензійних умов у редакції ПКМУ № 813 і Перелік посад МОЗ № 1065),
//   накладка майбутніх змін на ПКМУ № 285 у «Рентгені», два акти в реєстрі
//   НПА, тип 7 у Ctrl+K. Бамп обов'язковий: classifiers/data і rentgen/data
//   ходять без ?v= під cache-first.
// v38 (10.08.2026): кадрові вимоги пакетів тепер розбирає спільний модуль
//   pkg_staff.py — до того їх парсили двічі, різними алгоритмами, і числа не
//   збігалися (40 пакетів проти 41). Дані обох розділів («Посади» і
//   «Спеціальності та посади») перезібрано; вони ходять без ?v=, тож бамп
//   обов'язковий.
// v40 (10.08.2026): «Рентген» перебудовано навколо питання, а не переліку
//   актів: нова вкладка «Рішення» з розборами (analytics.json), адреса в
//   кожного екрана (#/case, #/pkg, #/doc) і компактний паспорт пакета.
//   У корпус доданий наказ ДІЯР № 138 — іменний перелік типів апаратів,
//   звільнених від ліцензування, через який виправлено твердження про
//   мамографи. rentgen/data ходить без ?v= під cache-first, тож бамп
//   обов'язковий, інакше сторінка малюватиме вчорашній набір даних.
// v41 (11.08.2026): під «Спеціальностями» і «Посадами» з'явився спільний шар —
//   граф кадрових сутностей (classifiers/data/kadry/). Обидві сторінки більше
//   не власники даних, а вью на нього; додано третю вкладку «Кадрові вимоги» зі
//   світлофором виконуваності. Дані кадрового контуру перезібрано (у «Посадах»
//   заодно виправлено назву, що показувалась капсом). Усе це ходить без ?v= під
//   cache-first, тож бамп обов'язковий: без нього сторінка малює вчорашній набір
//   і, наприклад, показує 0 замість 138 посад у пакетах.
// v42 (11.08.2026): нова сторінка classifiers/kadry.html — кадровий ланцюжок
//   від будь-якої точки (спеціальність, посада, характеристика, код, пакет).
//   У graph.json додано реєстр назв пакетів: повний граф має бути самодостатнім,
//   інакше інструмент, який читає лише його, шукає підпис вузла в іншому файлі.
//   graph.json ходить без ?v= під cache-first, тож бамп обов'язковий.
// v43 (11.08.2026): pkg_staff.py відновлює загублену скісну риску в парній
//   назві посади («Сестра медичнабрат медичний», пакети 8 і 63) — вимоги тепер
//   зіставлені всі 356/356, світлофор 332 ok / 24 warn, без risk. Дані kadry/,
//   spec/, posady/ перезібрано; виноска «Чому критична» стала на ширину всіх
//   чотирьох вікон. Дані ходять без ?v= під cache-first — бамп обов'язковий.
// v44 (12.08.2026): переупаковка «Посад» за аудитом — вузли dkhp отримали syn
//   (пошук «невролог» знаходить невропатолога) і perelik (позиція № 1065 у
//   паспорті), меню «Посади» перебудовано (auth-v2.js), view=codes став
//   реєстром усіх 286 кодів. Дані kadry/ перезібрано, ходять без ?v=.
// v45 (12.08.2026): атестаційний шар — наказ МОЗ № 650: новий тип вузла atest
//   (192 спеціальності + 51 профіль Номенклатури), ребра до посад Переліку і
//   Додатка 7, блок «Атестація і БПР» у паспортах посад і характеристик.
//   Дані kadry/ і atest/ ходять без ?v= під cache-first — бамп обов'язковий.
// v46 (14.08.2026): розділ «Тарифи 2025 ↔ 2026» (postanova/porivnyannya.html)
//   з даними postanova/data/comparison_2025_2026.json і додатковим джерелом
//   postanova/docs/postanova_1503.pdf, на який ведуть посилання «джерело 2025».
//   postanova/data ходить без ?v= під cache-first — бамп обов'язковий, інакше
//   перезбірка порівняння не доїхала б до того, хто вже відкривав розділ.
//   Заодно скидає збережені копії сторінок: у меню з'явився новий пункт, а
//   навігація віддає кеш, щойно мережа не вклалася в NAV_TIMEOUT_MS.
// v47 (14.08.2026): у «Тарифах 2025 ↔ 2026» вкладка ставок стала показувати
//   базовий тариф — по одному рядку на пакет, решта сум глави за кнопкою «ще N».
//   Перезібрано comparison_2025_2026.json (позначка базової ставки, типи ставок
//   за стемами, розрахункові тарифи глав 36 і 41–43). postanova/data ходить без
//   ?v= під cache-first — без бампу сторінка малювала б попередній набір.
// v48 (14.08.2026): у «Тарифах 2025 ↔ 2026» перейменовано підписи колонок
//   («Пункт 2025» читалося як «пункт № 2025») і перезібрано comparison_2025_2026
//   з новим правилом підпису «За що / одиниця»: беремо хвіст після суми, лише
//   коли він починається прийменником, інакше клітинка порожня.
// v49 (14.08.2026): у «Тарифах 2025 ↔ 2026» до коефіцієнтів додано ті, що названі
//   прозою в пунктах, — гірський, сільський, щільності, вікові, за готовність
//   тощо (18 глав, 63 рядки). Раніше вкладка показувала лише таблиці глав, тому
//   найвідоміших коефіцієнтів у порівнянні просто не було.
// v50 (14.08.2026): коефіцієнти з тексту пунктів переписано на розбір за
//   підпунктами — глава 3 давала 5 рядків замість ~40 (13 підпунктів п.36 і
//   18 підпунктів п.38 з ланцюжками, ступінчастими значеннями і відсиланнями
//   до додатків). comparison_2025_2026.json перезібрано: 421 рядок коефіцієнтів.
const CACHE = 'pmg-portal-v50';
// Скільки чекати на мережу під час навігації, перш ніж показати збережену копію.
// GitHub Pages під навантаженням віддає HTML секундами — без цього ліміту вкладка
// просто висить білою, і користувач тисне посилання вдруге.
const NAV_TIMEOUT_MS = 3500;
const CORE = [
  '/',
  '/index.html',
  '/portal.css',
  '/styles.css',
  '/auth-v2.css',
  '/brand.css',
  '/assets/icon-round-192.png',
  '/assets/bg-nszu-wide.webp',
  '/assets/nszu-shield.svg'
];

// ⚠️ НЕ додавати сюди skipWaiting() і clients.claim() у activate.
//
// Саме через них портал «відкривався з другого разу». Після кожного деплою
// сторінка, яку людина відкривала В ЦЮ МИТЬ, тягнула новий sw.js; той ставав
// активним негайно, старий воркер браузер обривав — а разом з ним і всі
// запити сторінки, які старий воркер саме обслуговував через respondWith.
// Сторінка отримувала «Failed to fetch» на своїх даних і малювала порожнечу
// («Не вдалося завантажити індекс постанови»). Друга спроба вже йшла через
// новий воркер і працювала. Відтворено локально 30.07.2026.
//
// Тепер новий воркер чекає у waiting і перебирає керування на НАСТУПНОМУ
// переході — сторінки в порталі й так перезавантажуються цілком, а свіжість
// файлів забезпечують версії ?v= в HTML, не воркер.
self.addEventListener('install', (e) => {
  e.waitUntil(
    caches.open(CACHE)
      .then((c) => c.addAll(CORE))
      .catch(() => {})
  );
});

self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys().then((keys) => Promise.all(
      keys.filter((k) => k.startsWith('pmg-portal-') && k !== CACHE).map((k) => caches.delete(k))
    ))
  );
});

// Кілька сторінок навмисно ходять по дані з міткою часу (?t=1785442158836),
// щоб обійти кеш. Для Cache Storage кожна така мітка — НОВИЙ ключ, тому за
// місяці користування там осідали сотні копій одного файлу і сховище пухло.
// Такі відповіді віддаємо з мережі, але не зберігаємо.
function isCacheBusted(url) {
  for (const value of url.searchParams.values()) {
    if (/^\d{13}$/.test(value)) return true; // Date.now()
  }
  return false;
}

self.addEventListener('fetch', (e) => {
  const req = e.request;
  if (req.method !== 'GET') return;

  const url = new URL(req.url);
  if (url.origin !== location.origin) return; // Supabase/CDN — напряму

  // Великі дані-шари LOINC (до ~17 МБ) — напряму в мережу, без клонування й
  // кешування в Cache Storage: інакше SW роздуває сховище і на слабких пристроях
  // може обірвати велике завантаження. HTTP-кеш браузера тут достатній.
  if (url.pathname.startsWith('/classifiers/data/loinc/loinc_data_')) return;

  if (req.mode === 'navigate') {
    // no-cache: GitHub Pages ставить max-age=600, без цього браузер до 10 хв
    // показує стару сторінку; ревалідація за ETag — дешева
    e.respondWith(navigateWithFallback(req));
    return;
  }

  // Дані роз'яснень оновлюються щодоби (звірка з архівом НСЗУ), тому тут
  // cache-first неприпустимий: користувач бачив би вчорашній стан аж до
  // другого заходу на сторінку. Мережа перша, кеш — запасний варіант.
  if (url.pathname.startsWith('/rozjasnennya/data/')) {
    e.respondWith(
      fetch(req, { cache: 'no-cache' })
        .then((res) => {
          if (res && res.ok) {
            const copy = res.clone();
            caches.open(CACHE).then((c) => c.put(req, copy));
          }
          return res;
        })
        .catch(() => caches.match(req))
    );
    return;
  }

  if (isCacheBusted(url)) return; // мітка часу — повз кеш, напряму в мережу

  e.respondWith(
    caches.match(req).then((cached) => {
      // Дані розділів (/…/data/…) ходять БЕЗ ?v=, тож фонове оновлення мусить
      // питати сервер, а не задовольнятися копією з HTTP-кешу — інакше в
      // Cache Storage лягає той самий учорашній файл. Для решти статики
      // ревалідація зайва: свіжість там забезпечує ?v= в HTML.
      const init = /\/data\//.test(url.pathname) ? { cache: "no-cache" } : undefined;
      const network = fetch(req, init)
        .then((res) => {
          if (!res || !res.ok) return res;
          const copy = res.clone();
          return caches.open(CACHE).then((c) => c.put(req, copy)).then(() => res, () => res);
        })
        .catch(() => cached);
      // Коли відповідь віддано з кешу, воркеру більше нема чого чекати — браузер
      // має право його вимкнути, і фонове оновлення не встигає дописатися в
      // Cache Storage. Через це перезібрані дані розділу не доїжджали навіть із
      // другого заходу: у кеші місяцями лишалася перша завантажена копія.
      // waitUntil тримає воркера живим саме до запису. (Спроба на випадок
      // браузерів, де подія вже неактивна: відповідь із кешу від цього не
      // залежить.)
      if (cached) {
        try { e.waitUntil(network.catch(() => {})); } catch (err) { /* подія вже закрита */ }
      }
      return cached || network;
    })
  );
});

// Навігація: мережа перша, але з таймаутом і БЕЗ підміни сторінки.
//
// Раніше на будь-яку помилку мережі віддавався '/index.html' — тож клік по
// «Постанові» на слабкому зв'язку мовчки показував головну під адресою
// /postanova/. Виглядало як «сторінка не відкрилась», людина тиснула вдруге,
// і другий раз спрацьовував. Тепер запасний варіант — збережена копія САМЕ
// цієї сторінки, а якщо її нема — чесне повідомлення про офлайн.
async function navigateWithFallback(req) {
  const cachedPage = () => caches.match(req, { ignoreSearch: true });

  // Запит один: при спрацюванні таймаута ми не кидаємо його, а лише перестаємо
  // чекати. Якщо збереженої копії немає — повертаємось до цієї ж обіцянки.
  const fromNetwork = fetch(req, { cache: 'no-cache' }).then((res) => {
    const copy = res.clone();
    caches.open(CACHE).then((c) => c.put(req, copy)).catch(() => {});
    return res;
  });
  // Якщо ми вже віддали копію з кешу, а мережа впаде пізніше — це не помилка
  // сторінки, тож глушимо, щоб не було unhandled rejection у воркері.
  fromNetwork.catch(() => {});

  let timer;
  const timeout = new Promise((resolve) => {
    timer = setTimeout(() => resolve(null), NAV_TIMEOUT_MS);
  });

  try {
    const res = await Promise.race([fromNetwork, timeout]);
    clearTimeout(timer);
    if (res) return res;
    // мережа не встигла — показуємо збережену сторінку, якщо вона є
    return (await cachedPage()) || (await fromNetwork);
  } catch (err) {
    clearTimeout(timer);
    const cached = await cachedPage();
    if (cached) return cached;
    return new Response(
      `<!doctype html><html lang="uk"><meta charset="utf-8">
       <meta name="viewport" content="width=device-width, initial-scale=1">
       <title>Немає зв'язку — НавігаторПМГ26</title>
       <body style="font-family:system-ui,sans-serif; max-width:520px; margin:18vh auto; padding:0 24px; text-align:center; color:#1f3347;">
         <h1 style="font-size:20px;">Сторінка недоступна офлайн</h1>
         <p style="color:#647688; line-height:1.6;">Цей розділ ще не збережено на пристрої, а зв'язку з мережею зараз немає.</p>
         <p><a href="/" style="color:#0284c7; font-weight:700;">На головну</a> &nbsp;·&nbsp;
            <a href="javascript:location.reload()" style="color:#0284c7; font-weight:700;">Спробувати ще раз</a></p>
       </body></html>`,
      { status: 503, headers: { 'Content-Type': 'text/html; charset=utf-8' } }
    );
  }
}

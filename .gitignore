__pycache__/
*.pyc

# Збиральний інструментарій, бінарники й бібліотека першоджерел — потрібні
# локально, але не публікуються на сайт. library/ винесено цілком 06.08.2026:
# 460 файлів (docx і pdf) на 105 МБ, на які не посилається жодна сторінка,
# роздували артефакт Pages до 126 МБ, і крок deploy падав за таймаутом 10 хв.
tools/
library/

# Векторна база ШІ-пошуку — лише локально (74 МБ, на сайті пошук офлайн-only)
data/embeddings.json

# Вихідні файли класифікаторів — читаються збиральними скриптами локально
Loinc_*.zip
nk-026_2021_.pdf
nk-024_2023.pdf
tabel-153_1998.html
tabel-951_2010.html
tabel-158_2005.html
tabel-1103_2020.html
tabel-995_2023.html
Національний класифікатор НК 025.pdf

# Логи збірки класифікаторів
classifiers/data/**/build_*.log
# Сіди з персональними даними (ПІБ, табельні номери, дати народження)
_seeds_local/

# Правила групера ДСГ: реконструкція, здобута зі статистики службової
# бази. Репозиторій публічний, тому в git їх немає — вони живуть у
# таблиці grouper_rules під RLS (див. upload_supabase.py).
koduvannia/data/grouper/
